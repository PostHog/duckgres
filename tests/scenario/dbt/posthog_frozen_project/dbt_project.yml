name: posthog_frozen_scenario
version: "1.0.0"
profile: duckgres_scenario

model-paths: ["models"]
test-paths: ["tests"]

models:
  posthog_frozen_scenario:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    facts:
      +materialized: table
    marts:
      +materialized: table
